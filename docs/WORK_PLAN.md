# 作業計画・現在地

最終更新: 2026-07-20
状態: `TODO`（未着手） / `IN PROGRESS`（進行中） / `BLOCKED`（外部条件待ち） / `DONE`（検証済み）

## 現在地

現在の主作業は、`xiao-boat-control-integration` を制御側XIAOとして統合することである。通信・記録側の完成ではない。制御側の既存コードはあるが、実機統合試験の完了は確認されていない。現時点で完了を主張できるのは、個別試験の記録がリポジトリに存在することと、ソースレベルの差分レビューを完了したことだけである。詳細は `INTEGRATION_GAP_REVIEW.md` を参照する。

**次に行う作業:** 制御側XIAOの統合に必要な差分を実装順へ分解する。まず、制御側の安全状態・手動試験・全センサ取得・UART送信について、計測に必要なタイムスタンプと診断を揃える。その後に、既に動作実績のある通信・記録側のUART→SD受信ロガーを接続して、制御側A〜Fの段階試験を行う。

## 実施順序

| 順序 | 作業 | 状態 | 完了条件・記録先 |
| --- | --- | --- | --- |
| 0 | プロジェクト文脈・Git管理・引継ぎ台帳を整備 | DONE | この3文書とルートREADME、AGENTS.mdに運用を記載 |
| 1 | 統合コードの差分レビュー | DONE（静的確認のみ） | `INTEGRATION_GAP_REVIEW.md` にピン、役割分離、プロトコル、ログ、計測指標、安全動作の差分を記録。ビルド・実機確認は未実施 |
| 2 | 制御側XIAOの統合実装 | IN PROGRESS | BNO08X、VL53L5CX、INA226、PCA9685、サーボ1個、VESC、UART送信、USB診断、安全状態機械を同時に成立させる |
| 3 | 制御側の受動統合計測（全センサ・出力なし） | TODO | センサ別Hz・最大間隔・I2C時間・UART・キュー・エラーを記録 |
| 4 | 制御側 B: サーボ中央保持 | TODO | センサへのサーボ電源由来の干渉を比較 |
| 5 | 制御側 C: サーボ安全範囲の低速往復 | TODO | 急反転500/2500 µsは使用しない |
| 6 | 制御側 D: VESC状態取得（Duty 0） | TODO | UART応答、タイムアウト、センサ干渉を記録 |
| 7 | 制御側 E: VESC 3% | TODO | 低Duty基準での連続動作と安全停止を確認 |
| 8 | 制御側 F: サーボ低速往復 + VESC 3% | TODO | 電流変動とセンサ欠損の相関を記録 |
| 9 | 通信・記録側の拡張 | TODO | 既存のUART→SD受信ロガーは基盤として使用する。GNSS、比較BNO、双方向コマンド、時刻同期、TXT／停止サマリー、解析を追加 |
| 10 | G: 2台・SD・Web UIを含む全体試験 | TODO | 5〜10分、時刻同期、全ログ、全停止動作を確認 |
| 11 | 計測ログのBIN→CSV変換・周期/遅延/欠損解析 | TODO | フィルタ・制御周期の根拠を作成 |
| 12 | 状態推定、ToF評価、ロール、舵、VESC、航法 | TODO | 11の根拠がそろった後にこの順で着手 |

## 試験ごとの必須判定

各試験で、センサ更新停止なし、SD書込みエラー0、UART CRCエラー0、ログ／キュードロップ0、異常な最大間隔なし、STOP/E-STOP正常、正常停止サマリー生成を確認する。満たせない場合は、`WORK_LOG.md` に事実・条件・次の切り分けを記録し、次段階へ進めない。
