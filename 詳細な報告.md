# 詳細な報告 — CoreS3連続SD記録とBNO08X生データ経路

最終更新: 2026-08-02

## 結論

CoreS3単体、およびXIAOとのUART接続のみ（記録なし）は、ともに安定して動作した。また、追加したBNO08XレポートをXIAOからCoreS3へ**記録せずに**送る条件でも、CoreS3の10秒ログ `RUN0007` は正常完走した。

一方で、`bno_capture=1` としてXIAOからBNO08X生データをCoreS3の記録キューへ投入する10秒試験では、開始直後にCoreS3のアプリケーション処理が停止した。Wi-Fiチップ全体の停止ではなく、ICMP pingには応答を継続したが、HTTP APIとアプリ診断は応答しなかった。そのため、依頼されていたBNO08Xの60秒静止性能試験は、まだ開始・成立していない。

現時点の最有力原因は、CoreS3のUART受信処理 `serviceControl()` が受信バイト数の上限なしに `while (controlUart.available())` を処理する構造であることである。BNO生データを記録する条件で入力量が増えると、UART受信FIFOが空にならず、メインループがUART処理から戻れない可能性が高い。これによりHTTP処理、60秒自動停止、finalize、診断表示が実行されなくなる。これはログ形式・SD設定・ESKF・UARTプロトコルの不整合を示すものではなく、CoreS3側のメインループ公平性の問題という仮説である。まだ修正・再試験での確証は得ていない。

## 安全状態と構成

- `shadow_only=true`、`actuator_output_enabled=false` を確認済み。実機アクチュエータへの出力は有効化していない。
- CoreS3: COM6、MAC `30:ed:a0:d4:bf:40`。
- XIAO ESP32S3: COM4、MAC `34:85:18:ab:fa:90`。
- UART配線: XIAO TX (D7) → CoreS3 RX (GPIO8)、CoreS3 TX (GPIO9) → XIAO RX (D6)、GND共通。双方はUSB給電で、5 V/3.3 Vは相互接続していない。
- CoreS3のSoftAP: `BOAT-ESKF-CORE`、HTTPは `http://192.168.4.1/`。
- UARTは921600 bps。CoreS3受信バッファは16 KiB。
- SD SPIクロックは10 MHz。
- LCDはSDとの共有SPI競合回避のため、`logging==true` または `logFinalizing==true` の間は `draw()` を呼ばない。`M5.update()`、UART、Web API、SDTRACE、ログ形式、バッファ、mutexは維持している。

## 実装済みの変更

### CoreS3のLCD停止

CoreS3のメインループでは、ログ中およびfinalize中だけLCD描画を停止するようにした。これはLCDとmicroSDが共有するSPIアクセスによる競合を避ける暫定対策である。画面停止中も `M5.update()` は継続する。

この変更後のCoreS3ビルドは、RAM 148,556 / 327,680 bytes、Flash 1,017,013 / 6,553,600 bytesであり、COM6へ書込み済みである。

### XIAOのBNO08Xレポート追加

既存のAccelerometer 100 Hz、Gyroscope Calibrated 100 Hz、Magnetic Field Calibrated 20 Hzを維持したうえで、次を追加した。

- Game Rotation Vector: 50 Hz（20,000 µs）
- Linear Acceleration: 50 Hz（20,000 µs）

XIAOからCoreS3へ送るBNOペイロードは固定長のまま、`kind` で種別を区別する。Game Rotation Vectorは `kind=3`、Linear Accelerationは `kind=5` である。XIAOビルドはRAM 111,724 / 327,680 bytes、Flash 561,809 / 3,342,336 bytesで、COM4へ書込み済みである。

なお、XIAOは `p1CaptureActive` がtrueのときだけ、これらのBNO生データをCoreS3へ転送する。CoreS3のログ開始APIで `bno_capture=1` を指定した場合にこの転送が開始される。

## SD連続記録の確定結果

### LCD描画ありの失敗とLCD停止の効果

以前の `RUN0004` はLCD描画を続けた条件で約16.78秒後にSD書込み障害を起こした。対して、LCD描画停止を適用した `RUN0005` は60秒を正常完走した。

- 記録数: 11,585
- BINサイズ: 1,566,502 bytes
- 512 B単位の書込み: 全件成功
- SDライブラリのCMDエラー: なし
- キュー残量: 最終0
- queue drop: なし
- flush、close、TXT生成: すべて成功
- BIN復号: 全件成功

したがって、従来のSD障害についてはLCD–SD共有SPI競合が有力な原因候補である。CoreS3では複雑なSPI排他実装を追加せず、当面はログ中およびfinalize中のLCD停止を維持する方針である。

## BNO追加後の切り分け結果

### 1. BNO生キャプチャを有効にした最初の60秒試行

`POST /api/log/start?duration_s=60&bno_capture=1` を送信した直後に、CoreS3のアプリ診断とHTTPが停止した。完全な生シリアルを取得できなかったため、この試行は原因解析試験として不成立であり、RUN番号・SDファイル・finalize結果は確定できない。

この時点では、追加したBNOデータがXIAOから送られ続けている状態でCoreS3を再書込みしても、起動直後に再発し得ると判断した。そのため、次の電源分離試験を行った。

### 2. CoreS3単体、XIAO TX切離し、ログなし

XIAO TX → CoreS3 RXを外し、両方を電源断した後、CoreS3のみをUSBから起動した。30秒以上、次をすべて満たした。

- アプリ診断を3行以上受信
- SoftAPへ接続可能
- ping応答あり（3/3、約1–3 ms）
- `/api/eskf` 応答あり
- CoreS3の異常停止なし

診断は `bytes=0 frames=0 log=0 q=0 rec=0 final=0` で、UART入力がない状態でCoreS3本体、Web、SoftAPが正常であることを示した。

### 3. UART完全接続、ログなし

配線を戻し、XIAOから通常のテレメトリを受けながら32秒以上ログを開始せずに動作させた。CoreS3は正常を維持し、Web APIとpingも正常だった。

観測値は、RX bytes が 1,443,878 → 2,021,382、frames が 10,538 → 14,750 と増加した。CRC error=0、COBS error=0である。`length_errors=1` と `sequence_gaps` の増加（2,754 → 2,774）は残っており、別途確認が必要だが、この条件でアプリ停止は起きなかった。

### 4. BNO追加データ送信中、BNO生キャプチャなし — RUN0007

次に、XIAO側が追加BNOレポートを有効にしたまま、CoreS3で `POST /api/log/start?duration_s=10&bno_capture=0` を送信した。APIはHTTP 202で開始を受理し、自動停止まで完走した。

| 項目 | 結果 |
| --- | --- |
| RUN | RUN0007 |
| 名目時間 | 10秒 |
| 記録数 | 1,918 |
| BINサイズ | 259,122 bytes |
| queue drop | 0 |
| queue high water | 10 |
| SD write error | 0 |
| 512 B write | 507回、すべて512/512 bytes成功 |
| 最終状態 | `log=0`, `q=0`, `final=0` |
| flush / close / TXT | 全て成功 |
| BIN復号 | 1,918 records、trailing 0で全件復号 |
| 記録時刻範囲 | キュー時刻で9,983,638 µs |

TXTには `normal_stop=1`、`control_frames=1443`、`control_crc_errors=0`、`control_cobs_errors=0`、`control_length_errors=1`、`gnss_nav_tx=2760` が記録された。この結果は、SD、writer、finalize、ログ形式が、BNO生キャプチャを開始しない条件では正常であることを示す。

### 5. BNO生キャプチャあり、10秒 — 最新の再現試験

完全なUART配線を維持したまま、`POST /api/log/start?duration_s=10&bno_capture=1` を送信した。HTTP 202の開始応答は保存でき、応答には `logging:true`、`duration_s:10`、`bno_capture:true` が含まれた。

開始直後、CoreS3のアプリ診断が止まり、保存された生シリアルは1,854 bytesで終了した。最後の記録は開始前の `log=0, rec=1918` の診断であり、`log=1`、自動停止、finalize、flush、close、TXT生成を示す行は取得できなかった。

10秒後の状態は次の通りである。

- ping: 3/3成功、約1 ms
- `/api/eskf`: 10秒でtimeout
- `/api/log/files`: 10秒でtimeout
- 停止API: HTTP応答を得られず、送信成功とは扱っていない
- RUN番号、BIN/TXT生成、SD内容: API不能のため未確認。`RUN0008` が生成されたとは記録しない。

この結果は、電源断やWi-Fi全体のクラッシュではなく、CoreS3アプリのメインループが進行しない状態と整合する。ICMPはWi-Fi側タスクで処理されるため、pingだけが生きている説明にもなる。

## 最有力原因の技術的根拠

CoreS3のメインループは概ね次の順に動く。

1. `M5.update()`
2. `web.handleClient()`
3. タッチ処理
4. 自動停止期限の確認
5. `serviceControl()` によるUART受信・COBS復号・キュー投入
6. GNSS、送信、ログwriter等の処理

現状の `serviceControl()` はUART受信について、利用可能なデータがなくなるまで無制限に繰り返す。追加後のBNOは最大でおおむね320フレーム/秒（Accel 100 + Gyro 100 + Mag 20 + Game Rotation 50 + Linear Acceleration 50）になり、他の制御・GNSS系フレームも重なる。`bno_capture=1` ではこれらのBNO生データもCoreS3の処理対象・記録対象になる。

受信処理が1周で戻らない場合、メインループ先頭のHTTP処理と自動停止判定へ戻れない。したがって、HTTPのtimeout、診断停止、ログの自動停止未実行という今回の観測と一致する。これが最有力の仮説である。

ただし、これはまだコード修正後の再現性試験で確定していない。SDカード、SPIクロック、バッファ容量、mutex、ログ形式、UART速度、COBS形式、ESKFの数式・設定は、この仮説検証のためにまだ変更していない。

## 未確定事項

- `bno_capture=1` の直後に、`serviceControl()` が実際に連続占有しているかを、受信処理に上限を設けた試験で確認する必要がある。
- UARTの `length_errors=1` と `sequence_gaps` の発生源は未特定である。CRC/COBSエラーは0であり、今回の直接原因と断定しない。
- BNO08XのQuaternion、Roll/Pitch/Yaw、gyro、acceleration、linear acceleration、accuracy、タイムスタンプ単調性、静止統計は、BNO生キャプチャの60秒完走後に初めて確定できる。
- 最後の `bno_capture=1` 試験ではCoreS3のHTTPが応答していないため、内部に未finalizeのファイルが残ったかどうかも未確認である。

## 次に行う安全な修正・検証案

提案する最小修正は、CoreS3の `serviceControl()` に1ループあたりの受信バイト数または復号フレーム数の上限を設け、必ずメインループへ制御を返すことである。これはXIAO側の受信処理と同様の公平化であり、UARTの物理設定、プロトコル、COBS、payload、SD設定、ログ形式、mutex、ESKF、アクチュエータ設定を変更しない。

修正後は、まず `bno_capture=1` の10秒試験を1回行い、HTTP、診断、自動停止、`q=0`、flush、close、TXT、BIN全件復号を確認する。これが成功した場合に限り、機体とBNO08Xを完全静止させた60秒試験へ進み、各BNO種別の実効Hz、姿勢・加速度・ジャイロ統計、accuracy、timestamp異常、ESKF状態、SD・UARTエラーを集計する。

## 保存済み証跡

- `pc-tools/boat_eskf/captures/LCD_OFF_SDTRACE_STATIC60_SUCCESS_20260802/` — RUN0005のLCD停止60秒SD成功証跡。
- `pc-tools/boat_eskf/captures/CORE_STANDALONE_20260802/` — XIAO TX切離しのCoreS3単体試験。
- `pc-tools/boat_eskf/captures/CORE_UART_NLOG_20260802/` — UART完全接続・ログなし試験。
- `pc-tools/boat_eskf/captures/BNO_EXTRA_NOCAP_LOG10_20260802/` — RUN0007、追加BNO有効・生キャプチャなしの10秒正常試験。
- `pc-tools/boat_eskf/captures/BNO_EXTRA_CAP_LOG10_20260802/` — 生キャプチャあり10秒再現試験の開始応答・生シリアル・ping/API timeout証跡。
- `docs/BNO_STATIC60_ATTEMPT_20260802.md` — 最初のBNO静止60秒試行の不成立記録。

---

# 追補: UART受信公平化修正後の10秒試験

最終更新: 2026-08-02

## 判定

CoreS3のUART受信処理に、指定どおり次の上限を実装した。

- 1回の呼出しあたり最大2,048 byte
- 最大32 decoded frame
- 最大2,000 µs

この修正により、`bno_capture=1` でもCoreS3のメインloopが完全に占有される状態は解消した。HTTP API、ping、1秒診断、自動停止、SdWriter、finalizeは継続した。

しかし、UART受信バッファが16 KiBに張り付き、時間上限到達が連続し、UART CRC/COBS/length errorと受信フレーム欠落が増加した。BNO生データの取得件数も期待周期を満たしていない。したがって、今回の10秒試験は**機能回復の確認には成功したが、添付指示の合格条件には不合格**であり、BNO08X 60秒静止試験へは進まない。

## 修正前の構造確認

- UART受信は独立FreeRTOSタスクではなく、CoreS3のArduino `loop()` 内から `serviceControl()` を呼ぶ構造だった。
- 修正前は `while(controlUart.available())` で受信データがなくなるまで無制限に処理していた。
- 修正前のbyte/frame/time上限はなかった。
- decoded frameのキュー投入は `enqueue()` 内の短いcritical sectionだけで、空き待ちをしない。キュー満杯時は`queueDrops`を増やして即時に戻るため、UART受信処理から見て非ブロッキングである。
- UART受信中にflush、close、直接SD書込みは行わない。SD書込みは`SdWriter`タスクが行う。
- CoreS3 RXバッファは16 KiB（`controlUart.setRxBufferSize(16384)`）。
- Arduino `loopTask` はESP32-S3 framework設定のCore 1、priority 1。`SdWriter` は `xTaskCreatePinnedToCore(..., priority=1, ..., core=1)` でCore 1、priority 1。
- XIAO側はBNOイベントタスクをCore 0、priority 3、link送信タスクをCore 0、priority 4で動かす。

## 実装差分の要点

`m5stack-cores3-telemetry-bridge/src/main.cpp` に次を追加した。

- `kControlRxByteBudget=2048`
- `kControlRxFrameBudget=32`
- `kControlRxTimeBudgetUs=2000`
- decoder状態を保持したまま、3条件のいずれかに達した時点で`serviceControl()`を抜ける。
- 受信バイト、decoded frame、BNO種別、処理時間、各予算到達回数、RXバッファ最大値、main loop最大周期、最大実行時間、free heap、minimum free heap、reset reasonを1秒診断へ追加。
- 既存のCRC/COBS/length判定、payload、COBS decoder、ログ形式、SD設定、mutex、BNO周期は変更していない。

CoreS3ビルドは成功し、RAM 148,668 / 327,680 bytes、Flash 1,018,865 / 6,553,600 bytesだった。PlatformIOのstub書込みはflash verification時に失敗したため、ROM no-stubでbootloader、partition、boot_app0、firmwareをCOM6へ書込み、各イメージのhash verifiedを確認した。

## 10秒試験条件

- CoreS3: 修正版、LCDはlogging/finalize中停止
- XIAO: BOAT_EXPERIMENT=24、Accel/Gyro 100 Hz、Magnetic 20 Hz、Game Rotation Vector 50 Hz、Linear Acceleration 50 Hz
- `POST /api/log/start?duration_s=10&bno_capture=1`
- UART 921600 bps、同一配線、同一microSD、SD SPI 10 MHz
- `shadow_only=true`、`actuator_output_enabled=false`
- 手動停止なし
- COM6とCOM4をDTR/RTS無効で同時接続し、ポートオープン後からfinalize後約15秒まで保存

## 実機結果

開始前にCore診断を3行受信し、開始後も約25.36秒間シリアル取得を継続した。開始API応答は次のとおりだった。

`{"logging":true,"duration_s":10,"bno_capture":true}`

### CoreS3の受信公平性

- `RXDIAG budget_hits`: 10秒ログ中も増加し、最終13,655。
- budget内訳: byte=0、frame=0、time=13,655。受信処理は時間条件で戻っていた。
- `controlRxMaxServiceUs`: 3,898 µs。これはFreeRTOS他タスクによる実測wall timeも含む値で、指定2,000 µsを超えた。CPU処理を返す構造にはなったが、実測時間上限の合格条件は満たしていない。
- `rx_buf_max`: 16,384 bytes。RXバッファが上限まで張り付き、受信処理能力が入力に追いついていない状態だった。
- logging中の`mainLoopMaxPeriodUs`: 81,806 µs、`mainLoopMaxExecutionUs`: 50,050 µs。メインloopは回ったが、余裕は十分ではない。
- `reset_reason=1`。試験中に再起動は観測していない。
- minimum free heap: 151,180 bytes。

### Web / 自動停止 / finalize

- 4、8、12、16、20秒時点の`/api/eskf`はすべて応答した。
- pingは3/3成功（1–3 ms）。
- 10秒で自動停止した。
- `SDTRACE`上で最終状態は`rec=470, q=0`。
- flush: `ok=1`。
- close: `ok=1`。
- TXT open/write/close: すべて`ok=1`。
- SDライブラリのCMD18/CMD0D/CMD00、`ff_sd_status`、`SD write failed`、panic行は捕捉されなかった。

### SD / BIN / TXT

- RUN番号: `RUN0009`
- BIN: 62,046 bytes
- TXT: 254 bytes
- records: 470
- queue_drops: 0
- queue_high_water: 8
- sd_write_errors: 0
- `SDTRACE write_calls=122`
- 保存されたwrite traceは10行で、全行 `req=512 actual=512 ok=1`
- flush/close後の最終キューは0
- BINは既存復号器で470件、62,046 / 62,046 bytes消費、trailing=0
- BINのqueue timestamp範囲は10,002,546 µs

TXTの`control_frames=21`、`control_crc_errors=3`、`control_cobs_errors=61`、`control_length_errors=68`は、Core起動後の累積decoderカウンタを記録する既存形式である。開始前から診断でCRC/COBS/lengthが少なくとも2/39/41あり、終了時は3/77/78になった。開始前から既にエラーが存在し、試験中にも増加したため、添付指示のUARTエラー0条件には不合格である。

### BNO・XIAO側

XIAOのUSB診断は試験中も継続した。BNOは`addr=0x4A`、nonfinite=0で、USB診断最終付近のイベント累計はAccel/Gyro/Mag=2940/2330/583だった。ただしXIAOの安全状態は`FAULT`であり、dry-runは1、アクチュエータ出力は無効だった。

XIAO側の`LINK queue/drop/high`は観測範囲でdrop=0、high-water=14だった。現在のUSB診断では`bnoLinkFrameDrops`自体を直接表示していないため、「BNO生送信drop=0」はこの試験証跡だけでは確定しない。

Core側のBINに含まれたBNO種別は少数で、全BNOレポートの期待周期を反映していない。Coreが有効なdecoded frameとして受け取った総数は、ログ開始後10秒で32 frameに留まった。したがって、Quaternion、Roll/Pitch/Yaw、gyro、acceleration、linear acceleration、accuracy、timestamp連続性の60秒統計は取得できていない。

## 合否一覧

| 条件 | 判定 | 根拠 |
| --- | --- | --- |
| bno_capture=1で10秒動作 | 合格 | 開始応答、10秒自動停止 |
| HTTP継続応答 | 合格 | 4/8/12/16/20秒API応答 |
| 診断途切れなし | 合格 | 開始前3行、終了まで23行 |
| 10秒自動停止 | 合格 | `rec=470, log=0`、finalize完了 |
| queue=0 | 合格 | flush/close前後`q=0` |
| queue drop=0 | 合格 | TXT/診断 |
| SD write error=0 | 合格 | TXT/SDTRACE |
| UART CRC/COBS/length error=0 | 不合格 | 終了値3/77/78、開始前から増加 |
| flush成功 | 合格 | SDTRACE `ok=1` |
| close成功 | 合格 | SDTRACE `ok=1` |
| TXT生成成功 | 合格 | RUN0009.TXT回収 |
| BIN全件復号 | 合格 | 470件、trailing=0 |
| CoreS3再起動なし | 合格 | reset_reason不変、シリアル連続 |
| XIAO BNO送信dropなし | 未確定 | 専用drop値がUSB診断に出ていない |
| BNO周期・姿勢統計 | 未成立 | decoded BNO件数不足 |

## 現時点の判定と次の分岐

今回の修正で「bno_capture=1開始直後にCoreS3アプリが完全停止する」現象は再現しなかった。従って、CoreS3の無制限`while(available())`がメインloop公平性を損なっていたという仮説は強く支持された。

ただし、16 KiB RXバッファ張り付き、CRC/COBS/lengthエラー増加、decoded frame不足が残った。現在は「メインloop starvation」は改善したが、「UART入力レートに対して2,000 µs受信予算で処理しきれず、入力ストリームが破損またはバッファオーバーフローする」段階へ切り分けが進んだと判断する。

本条件のまま60秒試験へ進むことは禁止する。次は、BNO周期・SD設定を変更せず、受信処理1回の実処理byte/frame数、UARTハードウェアoverflowの有無、XIAO側のBNO送信dropを可視化し、必要なら`bno_capture=1`でログキューへ投入しない診断分岐を追加して、UART単体の損失かログ投入経路の負荷かを分離する。

## 保存済み証跡

- `pc-tools/boat_eskf/captures/BNO_FAIR_RX_BUDGET_LOG10_20260802_RERUN/core_serial_raw.log`
- `pc-tools/boat_eskf/captures/BNO_FAIR_RX_BUDGET_LOG10_20260802_RERUN/xiao_serial_raw.log`
- `pc-tools/boat_eskf/captures/BNO_FAIR_RX_BUDGET_LOG10_20260802_RERUN/start.body`
- `pc-tools/boat_eskf/captures/BNO_FAIR_RX_BUDGET_LOG10_20260802_RERUN/eskf_04s.body` ～ `eskf_20s.body`
- `pc-tools/boat_eskf/captures/BNO_FAIR_RX_BUDGET_LOG10_20260802_RERUN/ping_during_start.txt`
- `pc-tools/boat_eskf/captures/BNO_FAIR_RX_BUDGET_LOG10_20260802_RERUN/RUN0009.BIN`
- `pc-tools/boat_eskf/captures/BNO_FAIR_RX_BUDGET_LOG10_20260802_RERUN/RUN0009.TXT`
- `pc-tools/boat_eskf/captures/BNO_FAIR_RX_BUDGET_LOG10_20260802_RERUN/MANIFEST.sha256`

---

# 追補 — 2026-08-03 BNO UART連番と周期の再診断

## 連番の原因と修正

RUN0031ではCoreの試験区間に2件の`seq_gap`があったが、XIAOのTX履歴で該当sequence 8157/8158はいずれも`allocated/enqueued/completed`（flags `0x07`）だった。8158が8157より先にキュー投入されたため、Coreが8158→8157→8159を受信して順不同を欠落として二重計上した。物理UARTの破損やSDの問題ではない。

XIAOの`linkSend()`を修正し、連番採番、COBS encode、TX FIFO投入を一つの短い`linkMux`区間にした。これによりsequenceとFIFO順を一致させた。UART速度、BNO設定、payload、SD、ログ形式、アクチュエータ設定は変更していない。

修正後、K4のRUN0032とK3のRUN0033、5報告同時のRUN0034はいずれも試験区間`seq_gap=0`、CRC/COBS/length=0/0/0、RX buffer full=0、XIAO送信完了数とCore受信数の一致を確認した。RUN0032–RUN0034はSD書込み、flush、close、TXT生成、BIN全件復号も成功した。

## XIAO書込み時の復旧

ROM書込みでQIOを強制すると、生成bootloaderの本来のDIOヘッダ（`E9 03 02 3F`）がQIO（`E9 03 00 3F`）へ変更され、XIAOがROM段階で`TG0WDT_SYS_RST`を繰返した。生成物本来のDIO設定で再書込みして復旧し、BNO08X 0x4A・dry=1・出力無効の起動を確認した。以後、COM4のXIAOを書き込む際はDIOを使う。

## 現在の判定

5報告同時のRUN0034は通信・記録経路としては合格した。ただし実効周波数はkind1=63.118 Hz、kind2=50.017 Hz、kind3=50.016 Hz、kind4=25.029 Hz、kind5=50.016 Hzである。kind1/4はこの試験で設定した50/20 Hzと一致しない。

このため、raw BNOのログenqueue試験と60秒静止試験には進んでいない。次はBNO08X側が受理・出力するkind1/4周期を設定変更なしで確認し、必要な変更が明確になった時点で承認を得る。

## 2026-08-03 — RUN0035: 全5 BNO報告・10秒 再試行（直接接続）

### 結論

RUN0035は、UART受信・SD連続記録・自動停止・finalize・BIN復号について有効な成功試験である。一方、BNO08Xの実効出力周期は kind 1（加速度）約63.2 Hz、kind 4（磁気）約25.0 Hzで、要求値50/20 Hzとは一致しない。この周期条件が未解決のため、raw BNOのログキュー投入および60秒静止試験には進まない。

### 対象機器と安全状態

- 使用したのは船用XIAO: COM4、MAC 34:85:18:AB:FA:90 と、船用CoreS3: COM6、MAC 30:ED:A0:D4:BF:40 のみ。
- 別用途のCoreS3用COM3は存在せず、検出・接続・書込みを一切行っていない。
- CoreS3 API確認時の状態は shadow_only=true、actuator_output_enabled=false。

### 今回判明したPC通信上の原因

最初の再試行はCoreS3故障ではなく、PCのcurlが 192.168.4.1 宛てのHTTPを学内Squidプロキシ経由にしていたことが原因だった。プロキシ経由では開始APIがタイムアウトし、502応答も混入した。この試験は無効として扱う。

以後のCoreS3 SoftAP APIでは NO_PROXY=* / no_proxy=* を設定するか、curlに --noproxy "*" を付け、Wi-Fi 2から192.168.4.1へ直接接続する。直接接続では /api/eskf がHTTP 200で応答した。

### CoreS3ファームウェア書込み

現行CoreS3ソースをビルドした。PlatformIOの通常書込みはスタブ起動後、実データ転送前に flash chip connection の検証失敗となった。この失敗ではファームウェアのデータは書かれていない。続いてCOM6だけにROM no-stub方式でアプリ領域0x10000を書込み、データハッシュ照合まで成功した。XIAOへの書込みは行っていない。

### RUN0035の実施条件

- API: POST /api/log/start?duration_s=10&bno_capture=1&bno_log_enqueue=0&uart_rx_diag=decode_count
- API開始応答: HTTP 202
- 自動停止: 成功。手動停止APIは送信していない。
- BNO rawフレームはCoreS3のSDキューへ投入しない診断モード。UART到達性・順序・周期を検証する条件。

### UART・順序・BNO受信結果

CoreS3の試験区間は sequence 58132から61875までで、開始ACKは58131、停止ACKは61876、境界は一致した。

| 項目 | 結果 |
| --- | --- |
| Core BNO kind 1/2/3/4/5 | 632 / 500 / 500 / 250 / 500 |
| XIAO TX done 1/2/3/4/5 | 632 / 500 / 500 / 250 / 500 |
| sequence gap | 0 |
| CRC / COBS / length error | 0 / 0 / 0 |
| RX buffer full | 0 |
| XIAO TX drop | 0 |
| XIAO request / enqueue / done | 各kindで完全一致 |

Core受信時刻の平均間隔は、kind1=15,801.96 us、kind2=20,004.10 us、kind3=19,983.87 us、kind4=39,953.86 us、kind5=19,982.03 usであった。これはkind1約63.2 Hz、kind4約25.0 Hz、kind2/3/5約50 Hzに対応する。

### SD・finalize・BIN検証

- RUN0035.BIN: 78,350 bytes
- RUN0035.TXT: 正常生成
- TXT records=573、queue_drops=0、sd_write_errors=0
- SDTRACE: 全512-byte writeは requested=512 / actual=512 / ok=1
- final queue=0
- flush=ok、close=ok、TXT open/write/close=ok
- BIN復号: 573 records、trailing bytes=0
- BIN内のrecord sequenceとqueue timestampは単調増加

### 保存場所

pc-tools/boat_eskf/captures/BNO_ALL_RETRY_DIRECT_20260803/

このディレクトリには開始API応答、生シリアル（CoreS3/XIAO）、経過中のESKF API応答、ファイル一覧、RUN0035.BIN、RUN0035.TXT、および取得メタデータを保存した。

### 次に行うこと

BNO08Xの設定値を変えずに、kind1とkind4が要求値と異なる周期で出力される理由を診断する。ユーザー承認なしに周期の受入条件を変更せず、また raw BNOログ投入や60秒静止データ取得へ進めない。
---

# 追補 — 2026-08-03 BNO08X受理周期（Feature Response）診断

## 結論

最終要求周期は**変更していない**。現在の全5種診断条件は、K1/K2/K3/K4/K5 = 50/50/50/20/50 Hz 要求（20,000/20,000/20,000/50,000/20,000 µs）である。

SH-2 Get Feature Response（0xFC）をBNO専用タスクから取得した結果、BNO08Xはこの要求を一律には受理していないことが確定した。

| 条件 | K1 Accel | K2 Gyro | K3 Game RV | K4 Mag | K5 Linear Accel |
| --- | ---: | ---: | ---: | ---: | ---: |
| ALL 5種同時（RUN0037）要求 | 50 | 50 | 50 | 20 | 50 Hz |
| ALL 5種同時（RUN0037）受理 | 62.5 | 50 | 50 | 25 | 50 Hz |
| K1単独（RUN0038）受理 | 62.5 | — | — | — | — |
| K4単独（RUN0039）受理 | — | — | — | 20 | — |

したがって、K1の62.5 Hzは単独でも再現するセンサ側受理値であり、K4の25 Hzは5種同時有効化時にだけ現れる組合せ依存の受理値である。CoreS3の到着時刻だけから推定したものではない。

最終要求周期（Accel/Gyroを100 Hzに戻すか、50 Hzを維持するか等）は未決定である。ユーザー承認なしに周期値を変更していない。raw BNOのSDログ投入および60秒静止試験へは進んでいない。

## 100/100/20/50/50 Hzから50/50/20/50/50 Hzになった経緯

- `BOAT_EXPERIMENT=24` は旧60秒静止試験用で、`src/bno_reader.cpp` のK1/K2だけを10,000 µsへ直接指定していた。従って要求はAccel=100 Hz、Gyro=100 Hz、Mag=20 Hz、Game RV=50 Hz、Linear Accel=50 Hzだった。
- 現在の `BOAT_EXPERIMENT=20` は全5種のUART順序・受信診断用で、`include/app_config.h` の `kAccelGyroIntervalUs=20,000` µsをK1/K2/K5に使う。K3は `kRotationIntervalUs=20,000` µs、K4は `kMagneticIntervalUs=50,000` µsである。
- Accel/Gyroを50 Hz要求へ下げた理由は、連番・FIFO順序・UART受信の診断時に負荷を抑えるための一時条件である。最終設計値を50 Hzへ変更したという意味ではない。

## 診断実装

XIAOの `bno_reader` に、各 `enableReport()` 成功直後、全report設定後、`wasReset()` による再設定後、P1試験開始直後に `sh2_getSensorConfig()` を呼ぶ診断を追加した。出力はreport ID、要求/応答時刻、requested/accepted interval・Hz、flags、change sensitivity、batch interval、sensor-specific、statusである。

`sh2_getSensorConfig()` はBNO専用タスク内でのみ呼ぶ。UART、SD、ログ形式、ESKF、アクチュエータ出力設定は変更していない。最初の実装はFeature Responseを全metricsへ4世代×5種保持してXIAOのloopTaskスタック余裕を約1 kB消費し、RUN0036でstack canary再起動を起こした。保存対象を生シリアルだけへ最小化して修正し、RUN0037以降は再起動なしで完走した。RUN0036は周期試験として不成立である。

起動直後のinitial取得はaccepted interval=0を返すが、直後に検出される`wasReset()`再設定後、および各P1開始直後のsnapshotでは有効な受理値を返す。本報告では後者を正本とする。すべてstatus=0、flags=0x00、change sensitivity=0、batch interval=0、sensor-specific=0だった。

## 実機結果

### RUN0037 — ALL 5種、Feature Response付き

- API: `POST /api/log/start?duration_s=10&bno_capture=1&bno_log_enqueue=0&uart_rx_diag=decode_count`
- HTTP 202、自動停止、SD flush/close/TXT成功、BIN復号成功。XIAO再起動なし。
- P1境界: start ACK=1621、first=1622、last=5367、stop ACK=5368。
- Core試験区間: BNO 632/500/501/250/500、sequence gap=0、CRC/COBS/length=0/0/0、RX buffer full=0。
- XIAO TX done: 632/500/501/250/500、drop/partial/zero=0。
- Core RX平均間隔（min/mean/max µs）: K1=1,635/15,833.87/64,807、K2=3,294/19,891.68/34,203、K3=5,065/19,988.15/69,837、K4=14,806/39,809.32/59,256、K5=5,858/19,876.52/30,840。
- XIAO RX平均間隔（min/mean/max µs）: K1=18/15,828.74/32,056、K2=9,234/19,984.45/31,502、K3=5,794/19,983.40/32,050、K4=28,919/39,990.98/51,253、K5=13,648/19,991.99/28,768。
- TXT: records=553、queue_drops=0、sd_write_errors=0。

### RUN0038 — K1単独

- Feature snapshot: requested=20,000 µs (50 Hz)、accepted=16,000 µs (62.5 Hz)。
- XIAO TX done=631、Core BNO=631、sequence gap=0、trial CRC/COBS/length=0/0/0、RX buffer full=0。
- XIAO RX間隔=11,190/15,823.37/20,407 µs、Core RX間隔=7,000/15,816.05/21,017 µs。
- SD/TXT/BIN: records=590、queue drops=0、SD error=0、normal stop。

### RUN0039 — K4単独

- Feature snapshot: requested=50,000 µs (20 Hz)、accepted=50,000 µs (20 Hz)。
- XIAO TX done=200、Core BNO=200、sequence gap=0、trial CRC/COBS/length=0/0/0、RX buffer full=0。
- XIAO RX間隔=47,732/49,958.86/51,982 µs、Core RX間隔=42,000/49,929.99/54,382 µs。
- SD/TXT/BIN: records=559、queue drops=0、SD error=0、normal stop。

## timestampと未達事項

既存の`sh2_SensorValue_t.timestamp`試験値はK1/K4で重複・逆行が多数あり、RUN0037でも全kindで単調増加を満たしていない。このフィールドをBNO内部時刻として解釈できるか、単位・wrap拡張を含めて未確定である。よって、要求された「BNO sensor timestampの単調増加・欠落・分布」の確定判定は未完了である。

また現在の時刻統計はmin/mean/maxであり、完全なdelta分布（分位点またはbin）は未実装である。最終周期の決定前に、BNO sensor timestampの形式確認・wrap拡張と、BNO/XIAO/Coreそれぞれのdelta分布診断を追加する必要がある。

Coreのtrial `unknown=1` はtype=23のUNKNOWNフレーム1件として残る。CRC/COBS/length=0とは独立であり、最終受入条件のunknown=0を満たす前に送信元と種別を特定する必要がある。

## 保存先と現在の書込み状態

- `pc-tools/boat_eskf/captures/BNO_FEATURE_ALL_20260803/` — RUN0036。不成立（初版metrics保持によるXIAO stack canary再起動）。
- `pc-tools/boat_eskf/captures/BNO_FEATURE_ALL_MINIMAL_20260803/` — RUN0037、ALL 5種の有効結果。
- `pc-tools/boat_eskf/captures/BNO_FEATURE_K1_20260803/` — RUN0038、K1単独。
- `pc-tools/boat_eskf/captures/BNO_FEATURE_K4_20260803/` — RUN0039、K4単独。
- 現在のCOM4 XIAO（MAC 34:85:18:AB:FA:90）には、全5種Feature Response診断版をDIOで復帰書込み済み（hash verified）。COM3は一切操作していない。
## 2026-08-03 — 最終周期の確定、timestamp/type=23診断、最終要求周期試験

### 1. 実施範囲と対象ポート

- 対象はボートXIAOのCOM4（USB ID 303A:1001、MAC `34:85:18:AB:FA:90`）とボートCoreS3のCOM6（MAC `30:ED:A0:D4:BF:40`）だけ。
- 別用途のCOM3（CoreS3 MAC `44:1B:F6:E2:09:F8`）は列挙・接続・書込みを行っていない。
- CoreS3のUART、SD 10 MHz、バッファ、mutex、BIN/TXT形式、START/STOP ACK境界、LCD停止条件、actuator_output_enabled=false、shadow_only=trueは維持した。

### 2. timestampの意味をコードで確定

`BNO08x`の`sh2_SensorEvent_t.timestamp_uS`はBNO内部クロックの生値ではない。Adafruitライブラリの`hal_getTimeUs()`が`millis()*1000`（uint32_t）をSH-2へ渡し、SH-2の`touSTimestamp()`がホスト読出し時刻、timestamp reference、report delay（100 us単位）を合成してuint64へ拡張している。したがって本ログの`timestamp_raw`は「SH-2が生成したホスト時刻推定値（us）」であり、BNO独立発振器時刻ではない。1つのSHTP payload内でreport delay補正が異なるため、kind別の並びでは同値・逆行が発生し得る。実際の到着時刻（XIAO `rxUs`、Core UART `core_rx`）とreport sequenceは全試験で単調であり、欠落は確認されなかった。

Core診断には`BNO_CORE_EVENT`（kind、report sequence、UART sequence、sensor_raw、sensor_converted、XIAO rx、queue push、Core rx）を追加した。`sensor_converted`は診断専用の32-bit wrap拡張値で、BIN payloadや制御処理は変更していない。

### 3. type=23の正体

共有enumのtype=23は`P1Capture`であり、START/STOP境界ACKの制御フレームである。Coreの`expectedPayloadLength()`と`typeName()`に`P1CaptureAckPayload`（16 byte）を登録し、`TYPE23_IDENTIFIED`で受信時刻、UART sequence、payload長/hex、CRC/COBS/length、直前type、boundary_ackを保存した。RUN0040〜RUN0046でtype23は毎回START/STOP ACKとして記録され、CRC/COBS/length=0/0/0、trial `unknown=0`。未知フレームではない。

### 4. K1/K4/ALL timestamp診断（最終周期変更前）

保存先は`pc-tools/boat_eskf/captures/BNO_TIMESTAMP_K1_20260803/`、`BNO_TIMESTAMP_K4_20260803/`、`BNO_TIMESTAMP_ALL_20260803/`。各フォルダに起動前からfinalize後70秒までのCore/XIAO生シリアル、開始API、BIN/TXT、`timestamp_analysis.json/txt`を保存した。

- K1（現行要求20,000 us）: Core/XIAO BNO=631/631、XIAO rx実効約63.1 Hz、report sequence欠落/重複/逆順=0、Core seq gap=0、CRC/COBS/length=0/0/0、RX full=0。
- K4（50,000 us）: BNO=200/200、XIAO rx実効約20.0 Hz、report sequence欠落/重複/逆順=0、Core seq gap=0、CRC/COBS/length=0/0/0、RX full=0。
- ALL（旧設定）: Core BNO=631/500/500/250/500、report sequence欠落/重複/逆順=0、Core seq gap=0、CRC/COBS/length=0/0/0、RX full=0。kind1/2/3/4/5の到着実効は約63.1/50.0/50.0/25.0/50.0 Hz。
- sensor timestampの同値/逆行は上記SH-2ホスト時刻推定の性質で説明できる。一方、XIAO rx、queue push、Core rxは全件で逆行/重複0。report sequenceは各kindでdelta=1、wrapのみ8-bit sequenceの通常wrap。

### 5. 最終要求周期の実装

診断通過後、既存環境を残したまま以下の4環境を追加した。

- `bno_final_k1_accel100_10s`: accelerometer要求100 Hz（10,000 us）
- `bno_final_k2_gyro100_10s`: calibrated gyro要求100 Hz（10,000 us）
- `bno_final_k4_magnetic20_10s`: calibrated magnetic要求20 Hz（50,000 us）
- `bno_final_all_100_100_20_50_50_10s`: accel/gyro/magnetic/game rotation/linear acceleration = 100/100/20/50/50 Hz要求

4環境ともビルド成功。書込み前に毎回COM4 MACを照合し、XIAO標準配置（bootloader 0x0、partition 0x8000、boot_app0 0xE000、app 0x10000）をDIOでhash verified書込みした。

### 6. 最終要求周期の実機結果

各RUNは同じ`POST /api/log/start?duration_s=10&bno_capture=1&bno_log_enqueue=0&uart_rx_diag=decode_count`（HTTP 202）で開始し、自動停止・finalize完了後にファイル一覧/BIN/TXTを取得した。手動停止は行っていない。

| RUN | 要求→Feature accepted | 実効Hz（XIAO） | Core BNO | seq/CRC/COBS/len | queue/drop/RXfull | flush/close/TXT | BIN |
|---|---|---:|---:|---|---|---|---|
| RUN0043 K1 | 100 → 125 Hz（8,000 us） | 126.244 | 1,261 | 0 / 0/0/0 | 0 / 0 / 0 | 成功 | 78,868 B、574 records、trailing 0 |
| RUN0044 K2 | 100 → 100 Hz（10,000 us） | 100.042 | 999 | 0 / 0/0/0 | 0 / 0 / 0 | 成功 | 81,486 B、591 records、trailing 0 |
| RUN0045 K4 | 20 → 20 Hz（50,000 us） | 20.015 | 200 | 0 / 0/0/0 | 0 / 0 / 0 | 成功 | 78,792 B、573 records、trailing 0 |
| RUN0046 ALL | accel 100→125、gyro 100→100、rotation 50→50、mag 20→25（40,000 us）、linear 50→50 | 126.342 / 100.107 / 50.019 / 25.004 / 50.019 | 1,264/1,001/501/251/501 | 0 / 0/0/0 | 0 / 0 / 0 | 成功 | 82,004 B、592 records、trailing 0 |

Feature Response statusは全件0、flags=0、batch=0、sensor_specific=0。XIAO側の`event_drop`、`tx_drop`、`tx partial/zero`はいずれも0。Core側のtrial `unknown=0`、`rx_buf_full=0`。TXTは全RUNで`normal_stop=1`、`sd_write_errors=0`、`control_crc_errors=0`、`control_cobs_errors=0`、`control_length_errors=0`。

ALL時の磁気だけは、50,000 usを要求してもFeature Responseが40,000 us（25 Hz）を返した。単独K4では50,000 us（20 Hz）を受理しているため、これは複数report同時設定時のBNO08X受理値として記録し、要求値と受理値を混同しない。要求周期そのものは変更していない。

### 7. 保存物

- `BNO_TIMESTAMP_K1_20260803/`, `BNO_TIMESTAMP_K4_20260803/`, `BNO_TIMESTAMP_ALL_20260803/`: timestamp/type23診断一式。
- `BNO_FINAL_K1_20260803/`（RUN0043）、`BNO_FINAL_K2_20260803/`（RUN0044）、`BNO_FINAL_K4_20260803/`（RUN0045）、`BNO_FINAL_ALL_20260803/`（RUN0046）: 生シリアル、API応答、SDTRACE、BIN/TXT、`timestamp_analysis.json/txt`、BIN復号JSON。
- `C:\tmp\run_bno_timestamp_diag.py`: 70秒取得ハーネス（proxy bypass、開始前診断確認、finalize後取得）。
- `C:\tmp\analyze_bno_timestamp.py`: Core BNOイベントの周期・stddev・重複・逆行・wrap・report sequence集計。
- `C:\tmp\verify_bno_bins.py`: BIN全件復号・trailing byte検証。

### 8. 現在の状態と次段階

現在COM4には最終ALL（100/100/20/50/50 Hz要求）ファームウェアを書込み済み。CoreS3 COM6にはtype23識別/BNO_CORE_EVENT診断を含むファームウェアを書込み済み。COM3は未操作。最終ALLの磁気受理値が25 Hzになる点を明示したうえで、通常運用側のセンサ・通信周期（control 100 Hz、PCA9685 50 Hz、ToF 10 Hz、INA target 50 Hz、AS5600 100 Hz、navigation 20 Hz、GNSS新fixのみ）へ進む段階である。
## 2026-08-03 — 最終ALL周期 raw BNO保存確認（RUN0047、dispatch、enqueue=1）

対象はCOM4（XIAO MAC 34:85:18:AB:FA:90）とCOM6（CoreS3 MAC 30:ED:A0:D4:BF:40）のみ。COM3は操作していない。現行最終ALLファームウェア・SD設定・UART・ログ形式を変更せず、指定API `POST /api/log/start?duration_s=10&bno_capture=1&bno_log_enqueue=1&uart_rx_diag=dispatch` をHTTP 202で1回実施した。開始時刻は保存済み `capture_meta.json` を参照。

要求／受理／実測周期（Hz）は次のとおり。

| kind | センサ | 要求 | Feature accepted | 実測（BNO_REPORT） |
|---:|---|---:|---:|---:|
| 1 | Accelerometer | 100 | 125（8,000 us） | 126.31 |
| 2 | Gyroscope | 100 | 100（10,000 us） | 100.03 |
| 3 | Game Rotation Vector | 50 | 50（20,000 us） | 50.016 |
| 4 | Magnetic Field | 20 | 25（40,000 us） | 25.008 |
| 5 | Linear Acceleration | 50 | 50（20,000 us） | 50.017 |

段階別件数はXIAO event／TX enqueue／TX complete／Core受信までは一致したが、BIN保存で欠落した。kind別は次のとおり。

| kind | XIAO event | TX enqueue | TX complete | Core受信 | BIN保存 | 差分 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 1261 | 1261 | 1261 | 1261 | 1258 | -3 |
| 2 | 1000 | 1000 | 1000 | 1000 | 997 | -3 |
| 3 | 500 | 500 | 500 | 500 | 499 | -1 |
| 4 | 250 | 250 | 250 | 250 | 250 | 0 |
| 5 | 499 | 499 | 499 | 499 | 498 | -1 |

Coreの `TRIAL_END` は `seq_gap=0`、CRC/COBS/length=0/0/0、unknown=0、bno_invalid=0、rxbuf_full=0。XIAO側もevent_drop=0、tx_drop=0、partial=0、zero=0。SDTRACEの512 B writeは記録された範囲で全て `ok=1 actual=512`、flush/close/TXT生成はok=1、TXTはRUN0047として生成された。BINは5,356 records、571,236 bytes、trailing byte=0、全件復号可能。ただしraw BNO段階別件数が一致していないため合格条件を満たさない。

キューは試験中最大81、STOP/finalize直後16、その後も27で固定し0にならなかった。`SDTASK max_process_us=255027`（SD書込み処理タスクの記録値）である。type=23は `P1Capture` として識別され、START/STOP ACK境界として `TYPE23_IDENTIFIED ... boundary_ack=1` を2件受信した。

判定：不合格。原因候補は自動停止/finalize境界でのSDキュー残留と、残留分のBIN未保存（kind 1/2/3/5）である。通常運用周期への切替は実施しない。再試験前にwriter停止条件、finalize開始条件、キュー排出待ち、BIN保存件数の整合を調査する。証跡は `pc-tools/boat_eskf/captures/BNO_RAW_ENQ_DISPATCH_ALL_RETRY_20260803/`（生シリアル、API応答、RUN0047.BIN/TXT、SDTRACE、`raw_dispatch_analysis.json/txt`）に保存した。初回のAPI timeout試行はログ開始されずRUN0046再取得のみであり、試験結果には含めない。
## RUN0047補足 — 判定根拠と保存物一覧

### 試験条件

- 実施日: 2026-08-03
- 対象: COM4 XIAO（MAC 34:85:18:AB:FA:90）、COM6 CoreS3（MAC 30:ED:A0:D4:BF:40）
- COM3: 接続・書込み・操作なし
- API: `POST /api/log/start?duration_s=10&bno_capture=1&bno_log_enqueue=1&uart_rx_diag=dispatch`
- API応答: HTTP 202、`logging=true`、`bno_capture=true`、`bno_log_enqueue=true`
- 既存の最終ALLファームウェア、UART、SD 10 MHz、バッファ、mutex、ログ形式は変更なし
- 初回のAPI timeout試行はSoftAP再起動直後であり、ログ開始されなかったためRUN0047の試験結果から除外した

### 周期の記録

要求周期を受理周期へ書き換えず、別々に記録した。

| kind | センサ | 要求周期 | Feature accepted周期 | BNO_REPORT実測周期 |
|---:|---|---:|---:|---:|
| 1 | Accelerometer | 100 Hz / 10,000 us | 125 Hz / 8,000 us | 126.31 Hz |
| 2 | Gyroscope | 100 Hz / 10,000 us | 100 Hz / 10,000 us | 100.03 Hz |
| 3 | Game Rotation Vector | 50 Hz / 20,000 us | 50 Hz / 20,000 us | 50.016 Hz |
| 4 | Magnetic Field | 20 Hz / 50,000 us | 25 Hz / 40,000 us | 25.008 Hz |
| 5 | Linear Acceleration | 50 Hz / 20,000 us | 50 Hz / 20,000 us | 50.017 Hz |

### 段階別件数の判定

XIAO event、XIAO TX enqueue、XIAO TX complete、Core受信は各kindで一致した。しかしBIN保存件数が一致しなかった。

| kind | XIAO event | TX enqueue | TX complete | Core受信 | BIN保存 | 欠落 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 1261 | 1261 | 1261 | 1261 | 1258 | 3 |
| 2 | 1000 | 1000 | 1000 | 1000 | 997 | 3 |
| 3 | 500 | 500 | 500 | 500 | 499 | 1 |
| 4 | 250 | 250 | 250 | 250 | 250 | 0 |
| 5 | 499 | 499 | 499 | 499 | 498 | 1 |

### 通信・SD・finalize診断

- `TRIAL_END`: `seq_gap=0`
- CRC/COBS/length: `0/0/0`
- unknown: 0
- BNO invalid: 0
- RX buffer full: 0
- XIAO event drop: 0
- XIAO TX drop: 0
- partial write: 0
- zero write: 0
- SD queue drop: 0
- SD write error: 0
- SDTRACEの512 B書込み: 記録範囲は全て `ok=1`, `actual=512`
- flush: `ok=1`
- close: `ok=1`
- TXT open/write/close: `ok=1`
- type=23: `P1Capture`としてSTART/STOP ACKを2件識別
- UART dispatch budget hit: 60（ただしCRC/COBS/length、drop、RX fullは0）

### queue残留と不合格理由

試験中のqueue最大値は81。自動停止後、finalize中の診断ではqueue=16となり、その後finalize終了後もqueue=27で固定した。要求条件の「queueが0になる」を満たしていない。

さらに、XIAOからCore受信までのraw BNO件数は一致しているのに、BIN保存だけがkind 1/2/3/5で合計8件不足した。したがって、今回の失敗はUART受信・CRC・SD write return値ではなく、自動停止からfinalize完了までのキュー排出とBIN保存の境界にある可能性が高い。

コード上、`stopLog()`は`logging=false`と`logFinalizing=true`を設定し、writerがキューを排出してflush/closeする。一方、finalize中も`enqueue()`は許可されるため、writerの排出処理とfinalize完了の間に新規フレームが追加され、close後にqueueが残る競合が考えられる。これは今回のログからの原因候補であり、修正済みとは扱わない。

### BIN/TXTと生ログ

- BIN: `RUN0047.BIN`
- TXT: `RUN0047.TXT`
- BINサイズ: 571,236 bytes
- BINレコード数: 5,356
- BIN consumed bytes: 571,236
- trailing byte: 0
- BIN全件復号: 成功
- 生Coreシリアル: `core_serial_raw.log`
- 生XIAOシリアル: `xiao_serial_raw.log`
- API開始応答: `start_response.json`, `start_response.headers`
- SDTRACE: `core_serial_raw.log`内の`SDTRACE`全行
- 集計: `raw_dispatch_analysis.json`, `raw_dispatch_analysis.txt`

### 最終判定

RUN0047は、通信・SD書込みの個別エラーは発生しなかったが、queue残留とBIN段階別件数不一致により完全合格ではない。通常運用周期への切り替えは保留する。次回は、writer停止条件、finalize開始条件、finalize完了前のenqueue抑止または完全排出待ち、BIN保存件数の一致を、設定変更を伴わない再現確認から調査する。
## RUN0048–RUN0049 finalize競合診断

### 目的と条件
COM4（ボート用XIAO、MAC 34:85:18:AB:FA:90）とCOM6（ボート用CoreS3、MAC 30:ED:A0:D4:BF:40）のみを使用した。COM3（別用途のCoreS3、MAC 44:1B:F6:E2:09:F8）は開かず、書込み・操作を行っていない。APIは `POST /api/log/start?duration_s=10&bno_capture=1&bno_log_enqueue=1&uart_rx_diag=dispatch`、SD 10 MHz、既存UART・BIN/TXT形式・バッファ・mutex・センサ周期を維持した。

RUN0048/0049では、Core受信までのraw BNO件数は揃っていたが、finalize開始時にwriterが終了し、その後もenqueueが受理される競合を診断した。RUN0049の診断値は `enqueue_accepted=5368`、`queue_dequeued=5357`、`written_to_bin=5357`、`pending_in_queue=11`、`writer_exit_count=1`、`enqueue_after_writer_exit=7`、`enqueue_after_file_close=7` であり、close後にtype 22/23/24/32/56/58が残った。これはSD write returnエラーではなく、enqueueゲートを閉じる前にwriter/finalizeを進めていた状態遷移上の競合と判定した。

## RUN0050–RUN0051 logger状態機械修正後の最終ALL 10秒試験

### 修正内容
- logger状態を `IDLE/RUNNING/STOP_REQUESTED/CLOSING_INPUT/DRAINING/FINALIZING/FINALIZED/ERROR` として記録。
- 自動停止で `STOP_REQUESTED` とし、P1Capture STOP ACK（type=23）を受信後にenqueueゲートを閉じる。
- ゲート閉鎖後は `DRAINING` とし、active enqueue=0、queue=0、BINバッファ=0になるまでwriterを終了しない。
- その後にflush、close、TXT生成を実行し、完了後 `FINALIZED` とする。
- close後のenqueue受理を禁止し、試行数は別カウンタで記録する。UART、SD設定、ログ形式、センサ周期、actuator出力条件は変更していない。

### RUN0051実施記録
- 実施日: 2026-08-03（JST）
- 開始API送信: 15:42:27.009 JST、HTTP 202
- 取得ディレクトリ: `pc-tools/boat_eskf/captures/BNO_FINALIZE_FIXED_ALL_FINAL_20260803/`
- 自動停止: P1Capture STOP ACK sequence=23670、最後のデータsequence=23669、ACK受信後にゲート閉鎖
- 実測ログ時間: XIAO trial RX timestampでkind1 10.002161 s（146,981,869→156,984,030 us）、kind2 10.007825 s。API指定は10 sで、自動停止が成立した。
- シリアル取得: Core 4,103行、XIAO 3,761行、reader errorなし。開始前診断はCore 25行、XIAO 105行。
- 試験終了後、BIN/TXTをfinalize完了後に取得した。

### 周期（要求値・Feature accepted値・実測値を分離）
| kind | センサ | 要求周期 | Feature accepted周期 | XIAO BNO_REPORT実測Hz | 試験event数 | TX enqueue | TX complete |
|---:|---|---:|---:|---:|---:|---:|---:|
| 1 | Accelerometer | 100 Hz (10,000 us) | 125 Hz (8,000 us) | 126.55 Hz | 1,266 | 1,266 | 1,266 |
| 2 | Gyroscope | 100 Hz (10,000 us) | 100 Hz (10,000 us) | 100.03 Hz | 1,001 | 1,001 | 1,001 |
| 3 | Game Rotation Vector | 50 Hz (20,000 us) | 50 Hz (20,000 us) | 50.04 Hz | 500 | 500 | 500 |
| 4 | Magnetic Field | 20 Hz (50,000 us) | 25 Hz (40,000 us) | 24.99 Hz | 250 | 250 | 250 |
| 5 | Linear Acceleration | 50 Hz (20,000 us) | 50 Hz (20,000 us) | 50.02 Hz | 501 | 501 | 501 |

要求値はaccepted値へ書き換えず、両者を別欄で保存した。kind 1/4はBNO08X受理値がそれぞれ125/25 Hzである。

### 段階別件数（完全一致）
| kind | XIAO event | XIAO TX enqueue | XIAO TX complete | Core受信 | BIN保存 |
|---:|---:|---:|---:|---:|---:|
| 1 | 1,266 | 1,266 | 1,266 | 1,266 | 1,266 |
| 2 | 1,001 | 1,001 | 1,001 | 1,001 | 1,001 |
| 3 | 500 | 500 | 500 | 500 | 500 |
| 4 | 250 | 250 | 250 | 250 | 250 |
| 5 | 501 | 501 | 501 | 501 | 501 |

### UART、キュー、SD、finalize
- UART sequence gap=0。
- CRC/COBS/length error=0、unknown=0、BNO invalid=0、RX buffer full=0。
- XIAO event drop/TX drop/partial/zero write=0。
- Core `LOGGER_COUNTERS`: `enqueue_attempt=5363`、`enqueue_accepted=5346`、`enqueue_rejected_inactive=17`、`enqueue_rejected_closing=17`、`queue_dequeued=5346`、`written_to_bin=5346`、`pending_in_queue=0`、`pending_bin_buffer=0`、`writer_exit_count=1`、`enqueue_after_writer_exit=0`、`enqueue_after_file_close=0`、`active_enqueue=0`、`gate_closed=1`。
- close後のenqueue試行は13回あったが、受理は0回（`enqueue_after_writer_exit=0`、`enqueue_after_file_close=0`）。
- `LOGGER_PENDING BEFORE_CLOSE q=0`、`LOGGER_PENDING AFTER_FINALIZE q=0`、`LOGGER_FINAL state=FINALIZED q=0`。
- SD queue drop=0、SD write error=0、512 B writeは全件 `ok=1 actual=512`。
- flush成功、close成功、TXT生成成功。`SDTASK max_process_us=336524`、`SDTRACE count=128 write_calls=1113`。
- type=23は `P1Capture` のSTART/STOP ACKとして識別され、`TYPE23_IDENTIFIED ... boundary_ack=1` を記録した。
- queue最大使用量およびwriter/SD処理最大値はCore診断行に保存した。RUN0051のfinalize時queue最大残量は0。

### BIN/TXT復号
- `RUN0051.BIN`: 569,660 bytes、5,346 records、consumed=569,660、trailing bytes=0、完全復号成功。
- BIN type別raw BNOはkind1/2/3/4/5 = 1,266/1,001/500/250/501で、XIAO event、TX enqueue、TX complete、Core受信と完全一致。
- `RUN0051.TXT`: 256 bytes、生成成功。
- BIN sequence範囲: 18866–23669。STOP ACKの直前まで保存され、欠落・重複・逆順はUART診断上0。

### 判定と次段階
RUN0051は、finalize競合修正後の完全合格と判定する。RUN0050も同一条件でqueue=0、全kind段階一致、BIN完全復号を満たしており、修正後2回連続で合格した。RUN0048/0049で確認した問題は、SDカードやCMDエラーではなく、enqueueゲート閉鎖前のwriter終了・finalize遷移競合だった。現行CoreS3ファームウェアはCOM6へ書込み済みで、COM3には触れていない。

この結果に基づき、次は通常運用周期へ切り替える前に、保存済みRUN0050/RUN0051を基準として60秒静止試験を実施し、BNO08Xの静止性能と長時間のfinalize再現性を確認する。XIAO/CoreのCOM割当とactuator_output_enabled=false、shadow_only=trueは維持する。
## RUN0052 VESC・PCA9685取り外し60秒静止ESKF試験

### 試験条件と対象

- 実施日: 2026-08-03（JST）
- 対象: COM4 ボート用XIAO（MAC 34:85:18:AB:FA:90）、COM6 ボート用CoreS3（MAC 30:ED:A0:D4:BF:40）
- COM3（MAC 44:1B:F6:E2:09:F8）は接続・シリアルオープン・リセット・書込み・操作を行っていない。
- VESCは電源線・信号線・UART線を取り外し、PCA9685はI2C・電源・サーボ接続を取り外した状態で実施した。試験中の再接続はない。
- XIAO起動診断は `dry=1`、Core `/api/eskf` は `shadow_only=true`、`actuator_output_enabled=false`。アクチュエータ出力は行っていない。
- RUN0051のCore修正版を維持し、再書込みは行っていない。UART、SD 10 MHz、COBS、CRC、BIN/TXT形式、logger状態機械、BNO要求周期は変更していない。
- API: `POST /api/log/start?duration_s=60&bno_capture=1&bno_log_enqueue=1&uart_rx_diag=dispatch`
- API応答: HTTP 202。開始時刻は15:12:18.788 JST。手動停止は送信していない。
- 証跡: `pc-tools/boat_eskf/captures/BNO_ESKF_STATIC60_20260803/`

### 自動停止と実測時間

P1Capture START ACKはsequence 98662、最初のデータは98663。STOP ACKはsequence 125696、最後のデータは125695で、境界は連続している。STOP ACKのCore受信時刻差は60.021867秒（77,403,828→137,425,695 us）。Core到着時刻から見たkind別区間は約59.941–60.038秒である。自動停止、STOP ACK受信、finalize開始・終了は成立した。

### 要求周期・受理周期・実測周期

| kind | センサ | 要求 | Feature accepted | XIAO実測 | Core到着平均間隔 |
|---:|---|---:|---:|---:|---:|
| 1 | Accelerometer | 100 Hz / 10,000 us | 125 Hz / 8,000 us | 126.326 Hz | 7,917.21 us |
| 2 | Gyroscope | 100 Hz / 10,000 us | 100 Hz / 10,000 us | 100.028 Hz | 9,989.33 us |
| 3 | Game Rotation Vector | 50 Hz / 20,000 us | 50 Hz / 20,000 us | 50.022 Hz | 19,979.45 us |
| 4 | Magnetic Field | 20 Hz / 50,000 us | 25 Hz / 40,000 us | 25.009 Hz | 39,958.92 us |
| 5 | Linear Acceleration | 50 Hz / 20,000 us | 50 Hz / 20,000 us | 50.018 Hz | 19,980.33 us |

要求値とaccepted値は別々に保存した。kind 1とkind 4は、従来どおりBNO08Xが要求値より速い125 Hz/25 Hzを受理している。

### 段階別件数

| kind | XIAO event | TX enqueue | TX complete | Core受信 | logger enqueue accepted | BIN保存 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 7,581 | 7,581 | 7,581 | 7,581 | 7,581 | 7,581 |
| 2 | 6,002 | 6,002 | 6,002 | 6,002 | 6,002 | 6,002 |
| 3 | 3,001 | 3,001 | 3,001 | 3,001 | 3,001 | 3,001 |
| 4 | 1,501 | 1,501 | 1,501 | 1,501 | 1,501 | 1,501 |
| 5 | 3,001 | 3,001 | 3,001 | 3,001 | 3,001 | 3,001 |

同一STOP ACK境界内で全段階の差分は0である。

### UART・sequence・SD・logger

- `TRIAL_END`: `seq_gap=0`、CRC/COBS/length=`0/0/0`、unknown=0、BNO invalid=0、RX buffer full=0。
- `SEQUENCE_STATE`: gaps=0、duplicates=0、out_of_order=0、wraps=0。BNO report sequenceは8-bit wrapを考慮するとkind 1～5すべて欠落・重複・逆順0。
- Core到着間隔のmin/mean/maxはkind1=`1,041/7,917.21/66,899 us`、kind2=`3,183/9,989.33/32,276 us`、kind3=`6,835/19,979.45/38,968 us`、kind4=`16,129/39,958.92/58,785 us`、kind5=`6,830/19,980.33/41,616 us`。
- `LOGGER_COUNTERS`: enqueue_attempt=27,736、accepted=27,724、rejected_inactive=12、rejected_closing=12、dequeued=27,724、written_to_bin=27,724、pending queue=0、pending BIN buffer=0、writer_exit_count=1、enqueue_after_writer_exit=0、enqueue_after_file_close=0。
- queue high-water=71。ゲート閉鎖後のenqueue試行6回はすべて拒否され、受理0回。
- `LOGGER_PENDING BEFORE_CLOSE q=0`、`AFTER_FINALIZE q=0`、`LOGGER_FINAL state=FINALIZED`。
- SD queue drop=0、SD write error=0、zero write=0。SDTRACEの記録済みwriteは要求512 Bとactual 512 Bが一致している。
- `LOGGER_TIMING`の `partial_writes=1` は、最終BINバッファが512 B未満だった内部flush回数であり、SD APIのshort writeではない。実際のSDTRACE writeは全件actual=requested。`max_write_chunk_us=100,344 us`、`max_flush_us=14,813 us`、`max_close_us=10,098 us`、`max_mutex_wait_us=251 us`。
- SDTASK max_process_us=310,193。flush、close、TXT生成、normal_stop=1、FINALIZEDを確認した。

### BIN/TXT

- `RUN0052.BIN`: 2,876,796 bytes、27,724 records、全件復号成功、consumed=2,876,796、trailing byte=0。
- BIN raw BNO kind 1/2/3/4/5は7,581/6,002/3,001/1,501/3,001で、全段階件数と一致する。
- `RUN0052.TXT`: 259 bytes、records=27,724、queue_drops=0、sd_write_errors=0、normal_stop=1。
- 初回HTTPダウンロードはCore Web応答切断でBINが2,264,400 bytesに短縮されたが、finalize後に再取得して完全な2,876,796 bytesへ更新した。最終検証は更新後ファイルに対して実施した。`RUN0052_analysis_summary.json`も同じフォルダへ保存した。

### ESKF保存状態

5/15/30/45/60秒の `/api/eskf` 応答を保存した。全時点で `eskf_initialized=true`、`finite=1`、`covariance_valid=1`、`reset_count=0`、`imu_gaps=0`、`time_reversals=0`。health=1だが、health_reasonは全時点で `mount_unvalidated`。GNSSは未受信（`gnss_age_us=UINT32_MAX`）、ToFは有効な静止基準として使われていない。

ESKF診断カウンタはP1開始前後の差分で、predict call +9,323、predict accept +8,995、reject +328。rejectの増分はすべてbad-dt（重複・逆順・stale・nonfinite・norm・queue-fullではない）である。update回数の累積カウンタは現行実装/APIに存在せず、`/api/eskf/innovations`は試験後も `received=false` であり、innovationの60秒時系列は未保存である。これは未実装項目として扱い、数値を推定していない。

### 姿勢・quaternion・bias・速度・位置の静止統計

APIスナップショットのうち15～60秒を定常区間の近似として計算した。高レートのESKF状態時系列ではなく、5点サンプリングによる統計である。

- quaternion norm: 5～60秒で0.99999998～1.00000025。正規化破綻なし。
- roll: 定常平均 -5.344235 deg、標準偏差0.045571 deg、範囲 -5.405284～-5.283015 deg、5→60秒変化 -0.150172 deg。
- pitch: 定常平均 -10.865013 deg、標準偏差0.150865 deg、範囲 -11.067883～-10.663088 deg、5→60秒変化 -0.495150 deg。
- yaw: 定常平均 -23.287898 deg、標準偏差0.206193 deg、範囲 -23.565067～-23.011819 deg、5→60秒変化 -0.676950 deg。
- gyro biasは `[0.00009, 0.00014, 0.00022] rad/s` で5～60秒の変化なし。
- accelerometer biasは `[0.00000, 0.00000, 0.11555] m/s²` で5～60秒の変化なし。
- velocityは5→60秒で `[+41.30103, -125.33264, +8.08374] m/s` 変化。
- NED位置は5→60秒で `[+19,899.61719, -114,850.12500, +2,018.01611] m` 変化。
- 最終covariance stddev配列は `[934175.875, 934175.875, 14969.375977, 1388.176514, 1388.176514, 77.157013, 272.012482, 272.012482, 272.012939, 1.065827, 1.065827, 0.241944, 0.201018, 0.201018, 0.201018]`。

姿勢quaternion normと有限性は維持されたが、位置・速度推定は静止条件として異常に大きくドリフトしている。5秒時点ですでに大きな累積NED/速度を持っていたため、絶対値は起動からの累積状態である。それでも5→60秒区間の増分が大きく、静止ESKF性能は合格とは判定しない。

### BNO raw値とtimestampの注意

XIAOの最終診断では、raw accel/gyro値はfiniteで、gyro平均はほぼ0 rad/s。ただしBNO sensor timestamp診断はkind 1～5で重複・逆行が発生している（kind1 dup=71/reverse=3482、kind2=41/2623、kind3=45/1562、kind4=14/730、kind5=32/1496）。これはreport sequenceの欠落ではなく、SH-2 timestamp fieldのwrap/補正値に関する既知の診断結果であり、CoreのUART sequenceは正常である。linear accelerationの軸別平均・標準偏差は現行XIAO診断出力に保存されていないため、未取得として明記する。

### 判定

- ロガー・UART・SD・BIN/TXT保存: 合格。
- ESKF finite/covariance/quaternion/reset: 合格。
- 静止時のESKF位置・速度ドリフト、bad-dt reject、innovation時系列未保存: 不合格／追加調査が必要。
- 本試験結果をもって通常運用周期への切替、アクチュエータ統合、航行試験へは進めない。
- 次の調査対象は、ESKF状態を試験開始時に明示リセットした場合の再試験、bad-dt rejectの発生源、ToF/GNSS未接続時の状態推定設計、およびinnovation/predict/updateカウンタの保存である。ファームウェア設定や閾値は、今回の結果だけで変更していない。
## RUN0053 ESKF??10????2026-08-03?

??????predict???dt??????????trace?eskf_reset_at_start?innovation/update counters???????XIAO COM4?MAC 34:85:18:AB:FA:90??CoreS3 COM6?MAC 30:ED:A0:D4:BF:40????????COM3?????MAC 44:1B:F6:E2:09:F8??????????XIAO?????????Core?stub??????ROM no-stub?bootloader/partition/boot_app0/firmware?hash verified???????

??API? HTTP 202 ?????
POST /api/log/start?duration_s=10&bno_capture=1&bno_log_enqueue=1&uart_rx_diag=dispatch&eskf_reset_at_start=1

??????Core 20??XIAO 104???????0?130??????????????????????Core?XIAO??????????Core??ESKF_RESET/P1 START_ACK/P1 STOP_ACK????XIAO???ESKF_RESET_BEFORE/AFTER?P1_START_ACK/P1_STOP_ACK?BNO_CAPTURE_REPORT?ESKF_TRACE_BEGIN/END??????????Core?TRIAL_START?rx_bytes=0 frames=0?TRIAL_END?frames=105?raw BNO kind1..5=0/0/0/0/0?TRIAL_STOP_ACK_TIMEOUT?LOGGER_STATE=ERROR?XIAO USB????BNO???????????????BNO???????Core TX?XIAO RX???????????????????

RUN0053.TXT?records=218?queue_drops=0?sd_write_errors=0?control_frames=105?CRC/COBS/length=0?SDTRACE 66??512 B write?req=512/actual=512?flush/close/TXT?????????raw BNO?0???ESKF????????????? pc-tools/boat_eskf/captures/BNO_ESKF_DIAGNOSTIC10_20260803/ ?????RUN0053_analysis_summary.json??????

RUN0052??????????????rec?Core BIN???????q?SD??????q=3?writer???????3???final=0/1?finalize???/???log=0?????logging=false??????stopLog()??enqueue gate???writer drain?flush?close?TXT????????????SD writer/finalize?????????RUN0052?logger/UART/SD/BIN/TXT?BNO????????????ESKF????????????predict accepted=8,995?bad-dt reject=328?innovation??????????

???RUN0053?ESKF?SD?dt???????????????Core TX?XIAO RX?XIAO TX?Core RX?GND?3???????P1 START_ACK????????????????10??????????
## 2026-08-03 作業再開：RUN0052再解析とRUN0053通信経路追跡

### 作業状態

- 現行正本は https://github.com/temesotejam/proglam.git の main とする。
- 作業ブランチ agent/bno-int-telemetry-handoff、HEAD 54d498fc2375ba29690e0453563a93ff9854ab3a。
- 作業開始時点で git status はクリーン。今回の解析ではコード・実機を変更していない。
- 対象はボート用XIAO COM4（既存識別MAC 34:85:18:AB:FA:90）とCoreS3 COM6（既存識別MAC 30:ED:A0:D4:BF:40）。COM3は操作していない。

### RUN0052非破壊再解析

証跡 pc-tools/boat_eskf/captures/BNO_ESKF_STATIC60_20260803/ の既存BIN、API応答、シリアルログ、解析JSONを上書きせず確認した。

- 実測制御区間：60,021,867 us。
- 要求周期 kind1..5：100/100/50/20/50 Hz。
- Feature accepted周期：125/100/50/25/50 Hz。
- XIAO実測Hz：約126.326/100.028/50.022/25.009/50.018 Hz。
- kind件数：7581/6002/3001/1501/3001。
- logger：enqueue accepted/dequeued/written = 27,724/27,724/27,724、pending=0、queue high-water=71、SD write error=0。
- UART：sequence gap/duplicate/out-of-order、CRC/COBS/length、unknown、RX buffer full = 0。
- BIN：2,876,796 bytes、27,724 records、trailing bytes=0、完全復号。
- ESKF：predict call差分9,323、accept差分8,995、reject差分328（全てbad-dt）。reset_count=0、finite=1、covariance_valid=1。
- P1開始時resetは実施されていないため、5秒時点の大きな状態値をRUN区間内の初期値と断定できない。eskf_reset_at_start=1 の有効試験が必要。
- innovation時系列は既存APIで received=false であり、RUN0052からは判定できない。

### RUN0053制御通信の最初の失敗位置

RUN0053証跡 pc-tools/boat_eskf/captures/BNO_ESKF_DIAGNOSTIC10_20260803/ を確認した。

- Core API開始応答はHTTP 202で、ESKF_RESET_AT_START queued=1 command_id=1533 が出力されている。
- Core側 TRIAL_START は rx_bytes=0 frames=0。試験中に受信したのはXIAOからの通常テレメトリのみで、BNO kindは0件。
- Core側最終値は TRIAL_END rx_bytes=15356 frames=105 bno_kind=0/0/0/0/0。CRC/COBS/length/unknown/RX fullは0。
- CoreはXIAO→Core方向を受信できているため、Core UART RXとXIAO TX方向は動作している。
- XIAO側ログにはBNO送信と起動診断はあるが、ESKF_RESET_BEFORE/AFTER、P1_START_ACK、P1_STOP_ACKが1件もない。XIAOの linkRxService() でCore→XIAOフレームが復号・dispatchされた証拠がない。
- よって最初の失敗位置は、Core sendControl()の呼出し後からXIAO linkUart.available()/linkDecoder.feed()入口まで。XIAO側のCRC/COBS/length不一致やcommand dispatch分岐まで到達していない。
- 同じプロトコル・同じUART設定でRUN0052、RUN0035等ではP1_START_ACK/P1_STOP_ACKが成立しているため、現時点でコード上のmessage type、packing、endianness、CRC、COBSの不一致は根拠がない。

### 次の実施条件

1. VESC/PCA9685未接続、actuator_output_enabled=false、shadow_only=true、DRY_RUNを維持する。
2. Core GPIO9（TX）→XIAO D6（RX）、XIAO D7（TX）→Core GPIO8（RX）、GND共通の配線を確認する。COM3には触れない。
3. 配線確認後、まずreset/START/STOPの短い双方向通信確認だけを行う。1つでも失敗したら10秒試験へ進まない。
4. 双方向確認が合格した場合のみ、RUN0054以降の新番号で開始時reset付き10秒静止試験を1回行う。RUN0053は再利用・上書きしない。
5. 60秒試験、通常運用周期、ZUPT/GNSS/ToF更新、アクチュエータ統合にはまだ進まない。
# 2026-08-03 CONTROL_LINK_CHECK2 / RUN0055

## 目的
RUN0054で発生したXIAO Bnoタスクのstack canary再起動を、通信条件を変えずに再確認した。対象はボート用XIAO COM4（MAC 34:85:18:AB:FA:90）とCoreS3 COM6（MAC 30:ED:A0:D4:BF:40）のみで、COM3は未接続・未操作。

## 実施前の修正
XIAO `bnoTask` のFreeRTOSスタックを4096 wordから8192 wordへ増量した。理由は、Bnoタスク内の既存ESKF 15状態共分散伝播（複数の15x15自動行列）と診断スナップショット／trace呼出しの合成スタックが4096 wordを越え、RUN0054でstack canary再起動したため。BNO周期、UART、ESKF演算、SD、mutex、ログ形式、アクチュエータ設定は変更していない。

## ビルド・書込み
- XIAO環境 `bno_final_all_100_100_20_50_50_10s`: build成功（RAM 204308/327680、Flash 576429/3342336）、COM4書込み・hash verified。
- Core環境 `m5stack-cores3`: build成功（RAM 168076/327680、Flash 1032377/6553600）、COM6 ROM no-stub/DIO書込み・hash verified。

## 短時間確認
POST `/api/log/start?duration_s=1&bno_capture=1&bno_log_enqueue=0&uart_rx_diag=dispatch&eskf_reset_at_start=1` はHTTP 202、`eskf_reset_queued=true`。RUN番号はRUN0055。

- XIAO `P1_START_ACK` と `P1_STOP_ACK`: 各1。
- Core `TRIAL_START_ACK` と `TRIAL_STOP_ACK`: 各1。
- Core trial: `seq_gap=0`、CRC/COBS/length=0/0/0、RX buffer full=0、BNO kind 1..5 = 127/100/50/25/50。
- XIAO TX: kind 1..5のenqueue/doneは127/100/50/25/50、drop=0、event queue drop=0。
- ESKF API: `reset_count=1`、`shadow_only=true`、`actuator_output_enabled=false`、`xiao_link.sequence_gaps=0`、CRC/COBS/length=0/0/0。
- logger: `enqueue_accepted=115`、`queue_dequeued=115`、`written_to_bin=115`、`pending_in_queue=0`、`LOGGER_FINAL state=FINALIZED q=0`、flush/close/summary TXT open/write/close成功。RUN0055.BIN=16758 bytes、TXT=253 bytes。
- stack overflow/Guru Meditation/reboot: Core/XIAO双方0件。RUN0054のSTOP ACK timeoutは再現しなかった。
- 参考診断: loggerの短時間試験では`partial_writes=1`が記録されたため、10秒合格判定では512 B書込み全件とpartial/zeroを改めて確認する。これは通信確認を不合格とするものではない。

## 証跡
`pc-tools/boat_eskf/captures/CONTROL_LINK_CHECK2_20260803/` に開始応答、ESKF/link/files API応答、Core/XIAO生シリアル、capture_metaを保存。RUN0055は短時間通信確認用であり、10秒raw BNO試験の結果ではない。

## 判定と次段階
START/STOP/ACK、ESKF reset、finalize、再起動なしの短時間通信条件は合格。次は同じファームウェア・同じ周期・同じ安全条件で、要求された10秒試験を1回だけ実施し、kind 1～5のevent→TX enqueue→TX complete→Core受信→BIN保存の段階差、partial/zero write、queue drop、BIN復号を確認する。合格するまで通常運用周期へ切り替えない。
# 2026-08-03 RUN0057：最終ALL raw BNO 10秒再試験

## 条件と対象
- 対象：ボート用XIAO COM4（MAC 34:85:18:AB:FA:90）／ボート用CoreS3 COM6（MAC 30:ED:A0:D4:BF:40）。COM3は未接続・未操作。
- API：`POST /api/log/start?duration_s=10&bno_capture=1&bno_log_enqueue=1&uart_rx_diag=dispatch`
- 自動停止を待ち、手動STOPは送信していない。RUN番号はRUN0057。
- `shadow_only=true`、`actuator_output_enabled=false`を維持。

## 要求・受理・実測周期
| kind | センサ | 要求 | Feature accepted | XIAO実測Hz |
|---:|---|---:|---:|---:|
| 1 | Accelerometer | 100 Hz (10000 us) | 125 Hz (8000 us) | 約126.3 Hz |
| 2 | Gyroscope | 100 Hz (10000 us) | 100 Hz (10000 us) | 約100.02 Hz |
| 3 | Game Rotation Vector | 50 Hz (20000 us) | 50 Hz (20000 us) | 約50.02 Hz |
| 4 | Magnetic Field | 20 Hz (50000 us) | 25 Hz (40000 us) | 約25.01 Hz |
| 5 | Linear Acceleration | 50 Hz (20000 us) | 50 Hz (20000 us) | 約50.02 Hz |

Feature ResponseはXIAO生シリアルの`BNO_FEATURE phase=snapshot point=after_all`を根拠にした。要求値を書き換えず、受理値と実測値を別に保存している。

## 件数の段階一致
XIAO BNO event／TX enqueue／TX complete／Core受信／BIN保存（kind別）は次のとおりで、各段階差は0。

- kind 1: 1263 / 1263 / 1263 / 1263 / 1263
- kind 2: 999 / 999 / 999 / 999 / 999
- kind 3: 499 / 499 / 499 / 499 / 499
- kind 4: 250 / 250 / 250 / 250 / 250
- kind 5: 500 / 500 / 500 / 500 / 500

Core `TRIAL_END`は`bno_kind=1263/999/499/250/500`、XIAO `BNO_TX`も同値。Core受信の型別集計はtype 2=1263、type 3=999、type 4=999（kind 3+5）、type 20=250。

## 通信・キュー・SD・finalize
- UART sequence：`first=23758 last=28260 valid=1`、`SEQUENCE_STATE gaps=0 duplicates=0 out_of_order=0 wraps=0`。
- report_seqはCoreの`BNO_CORE_EVENT`をkind別・8-bit wrap考慮で再計算し、欠落0、重複0、逆順0。
- UART CRC/COBS/length=0/0/0、unknown=0、bno_invalid=0、RX buffer full=0。
- XIAO event/TX drop=0、partial/zero UART write=0、BNO event queue drop=0。
- SD queue drop=0、SD write error=0。`LOGGER_COUNTERS enqueue_accepted=4620 queue_dequeued=4620 written_to_bin=4620 pending_in_queue=0`。
- `LOGGER_TIMING partial_writes=0 zero_writes=0`。max SD write chunk=31526 us、max queue wait=124797 us、max flush=12394 us、max close=10399 us、max mutex wait=77 us。queue high-water=79。
- SDTRACEの512 B書込みは全件`ok=1 actual=512`。flush、close、TXT open/write/closeは成功。
- 自動停止、`TRIAL_STOP_ACK`、`LOGGER_FINAL state=FINALIZED q=0`を確認。type=23 P1Capture ACKはAPI link上でSTART/STOP各1。

## BIN/TXT復号
- `RUN0057.BIN`を保存し、既存`boat_eskf.binlog.records()`で4620件を復号。trailing byte=0。
- BIN raw BNO kind：kind1=1263、kind2=999、kind3=499、kind4=250、kind5=500。段階件数と一致。
- `RUN0057.TXT`：records=4620、queue_drops=0、sd_write_errors=0、control_frames=4512、control_crc/cobs/length=0/0/0。

## 時間・timestamp・状態
- Core到着時間（kind1 first→last）：9,994,216 us（約9.994 s）。XIAO RX時間（kind1 first→last）：9,994,931 us（約9.995 s）。
- P1 capture境界：first sequence 23758、last 28260。
- SH-2 raw sensor timestampは既知の仕様・HAL由来の重複／逆行が残る（XIAO BNO_TS_TRIAL：kind1 dup=8/reverse=589、kind2=8/435、kind3=8/260、kind4=7/131、kind5=9/255）。これはreport_seq/UART sequenceの判定とは別の値であり、ESKF dt源には直接使わない。
- 試験後APIは`shadow_only=true`、`actuator_output_enabled=false`、link sequence_gaps=0、CRC/COBS/length=0。今回の指定APIでは`eskf_reset_at_start=false`のため`reset_count=0`で、ESKFのNED/速度は試験前からの累積状態として記録した。
- XIAO/Core双方にstack overflow、Guru Meditation、再起動なし。

## 判定
RUN0057は、今回指定されたALL raw BNO 10秒試験の合格条件（段階件数一致、UART/report sequence、drop/error、queue、partial/zero、auto stop、flush/close/TXT、BIN全件復号）を満たした。RUN0056の`partial_writes=2`は正常な最終短チャンクの誤カウントだったため、Core側カウンタを実actual<nだけ数えるよう修正してからRUN0057を再試験した。

通常運用周期への切替は、別途ユーザー確認を取るまで実施していない。raw sensor timestampの重複／逆行と、resetなし試験でのESKF累積ドリフトは、次の姿勢軸・ESKF評価で扱う。

## 証跡
`pc-tools/boat_eskf/captures/BNO_ALL_RAW_ENQ_10S_RETRY_20260803/` に開始API応答、終了後API応答、Core/XIAO生シリアル、RUN0057.BIN、RUN0057.TXT、capture metadataを保存。比較用に`BNO_ALL_RAW_ENQ_10S_20260803/`（RUN0056）も保持。

## 2026-08-03 RUN0058 Game Rotation Vector���m�F����

- ���m�F�p��BOAT_EXPERIMENT=21�iGyro 100 Hz + Game Rotation Vector 100 Hz�j��COM4�֏����݁BCOM6��API�̂݁ACOM3�͖�����B
- RUN0058��10�b������~�ABIN 332,752 bytes�A2,976 records�Atrailing=0�ATXT records=2,976�Bkind2=999�Akind3=999�BP1Capture ACK=2�AUART sequence gap/CRC/COBS/length=0�Aqueue drop/SD error=0�B
- Game Rotation Vector�Î~���ς�q=[-0.011292,0.998840,-0.044754,0.013270]�ARoll=-174.851632 deg�APitch=1.461164 deg�AYaw=-178.638889 deg�Bquaternion norm����0.999994�B
- raw sensor timestamp�͊��m��HAL�R���ŋt�s����ikind2=354�Akind3=371�j�Breport sequence��BIN�����͐���B
- �@�̂���]�����鎲�E���������͖����{�B���Ɉꎲ����]�����A�I����BOAT_EXPERIMENT=23�֖߂��B�ڍ�: docs/BNO_ATTITUDE_GVR_10S_20260803.md


## RUN0059 BNO姿勢軸確認（2026-08-03）

詳細は docs/BNO_AXIS_TEST_RUN0059_20260803.md を参照。60秒自動停止・BIN/TXT/finalize・SD/UART経路は正常。Gyro/GVR各5888件、queue drop/SD/UARTエラー0、BIN trailing 0。姿勢値は大きく変化したが、約1.15秒のcallback欠測とreport sequenceリセットが1回あり、軸符号判定は部分成立として再試験待ち。
